# ~/.config/uwsm/env-hyprland
# Format: shell `export KEY=VALUE`
# Scope: Only when the session is Hyprland under uwsm.

# Hyprland-specific toggles. Keep minimal.
export HYPRLAND_TRACE=0

# If you ever need to pin device preference for games/tools that look at AQ_*:
# export AQ_DRM_DEVICES=/dev/dri/card1:/dev/dri/card0

# If you decide to enable client-side decorations for Qt apps only under Hyprland:
# export QT_WAYLAND_DISABLE_WINDOWDECORATION=1